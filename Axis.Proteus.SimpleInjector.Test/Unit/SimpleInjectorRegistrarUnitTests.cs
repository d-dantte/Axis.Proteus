using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Axis.Proteus.SimpleInjector.Test.Unit
{
    [TestClass]
    public class SimpleInjectorRegistrarUnitTests
    {

        [TestMethod]
        public void RegisterTR_WithValidArguments_ShouldRegisterType()
        {
            //var container = new Container();
            //var registrar = new SimpleInjectorRegistrar(container);

            //_ = registrar.Register(typeof(C_I1), RegistryScope.Transient);

            //Assert.IsTrue(container
            //    .GetCurrentRegistrations()
            //    .Where(ip => ip.ServiceType == typeof(C_I1))
            //    .Where(ip => ip.ImplementationType == typeof(C_I1))
            //    .Where(ip => ip.Lifestyle.Equals(Lifestyle.Transient))
            //    .Any());
        }

    }
}
